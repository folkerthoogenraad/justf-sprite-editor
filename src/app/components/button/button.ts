import { Component, input } from '@angular/core';

@Component({
  selector: 'c-button',
  imports: [],
  templateUrl: './button.html',
  styleUrl: './button.scss',
  host: { class: "button" }
})
export class Button {
  
}
